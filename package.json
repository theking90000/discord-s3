{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.5.2"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "dev:run": "lerna run build && node packages/discord-file-upload/dist"
  }
}
